# Carte BMW iX3 avec Contrôles Simples (Composants Natifs)
# Aucune dépendance HACS requise

type: vertical-stack
cards:
  # En-tête
  - type: markdown
    content: |
      ## 🎛️ Contrôles BMW iX3
      **SOC Actuel** : {{ states('sensor.state_of_charge_last_known') }}% | 
      **SOC Cible** : {{ states('number.bmw_ix3_target_soc') or '80' }}%
    card_mod:
      style: |
        ha-card {
          background: linear-gradient(135deg, #2196F3 0%, #1976D2 100%);
          color: white;
          padding: 20px;
          border-radius: 15px;
        }
        ha-markdown {
          color: white;
        }
        h2 {
          color: white;
          margin: 0;
        }
  
  # Contrôles principaux
  - type: entities
    title: 🎯 Réglages Principaux
    entities:
      - entity: number.bmw_ix3_target_soc
        name: SOC Cible
        icon: mdi:battery-charging
      - entity: number.bmw_ix3_departure_time
        name: Heure de Départ
        icon: mdi:clock-outline
      - entity: switch.bmw_ix3_auto_stop_80_percent
        name: Arrêt Auto à 80%
        icon: mdi:battery-80
    card_mod:
      style: |
        ha-card {
          background: rgba(255, 255, 255, 0.1);
          backdrop-filter: blur(10px);
          border-radius: 15px;
        }
  
  # Boutons d'action rapide SOC
  - type: horizontal-stack
    cards:
      - type: button
        name: SOC 80%
        icon: mdi:battery-80
        tap_action:
          action: call-service
          service: number.set_value
          service_data:
            entity_id: number.bmw_ix3_target_soc
            value: 80
        card_mod:
          style: |
            ha-card {
              background: rgba(76, 175, 80, 0.2);
              border-radius: 15px;
              color: white;
              border: 2px solid rgba(76, 175, 80, 0.5);
            }
            ha-button {
              color: white;
            }
      - type: button
        name: SOC 90%
        icon: mdi:battery-90
        tap_action:
          action: call-service
          service: number.set_value
          service_data:
            entity_id: number.bmw_ix3_target_soc
            value: 90
        card_mod:
          style: |
            ha-card {
              background: rgba(255, 193, 7, 0.2);
              border-radius: 15px;
              color: white;
              border: 2px solid rgba(255, 193, 7, 0.5);
            }
            ha-button {
              color: white;
            }
      - type: button
        name: SOC 100%
        icon: mdi:battery
        tap_action:
          action: call-service
          service: number.set_value
          service_data:
            entity_id: number.bmw_ix3_target_soc
            value: 100
        card_mod:
          style: |
            ha-card {
              background: rgba(244, 67, 54, 0.2);
              border-radius: 15px;
              color: white;
              border: 2px solid rgba(244, 67, 54, 0.5);
            }
            ha-button {
              color: white;
            }
  
  # Boutons d'heure de départ
  - type: horizontal-stack
    cards:
      - type: button
        name: Départ 06h00
        icon: mdi:clock-fast
        tap_action:
          action: call-service
          service: number.set_value
          service_data:
            entity_id: number.bmw_ix3_departure_time
            value: 6
        card_mod:
          style: |
            ha-card {
              background: rgba(76, 175, 80, 0.2);
              border-radius: 15px;
              color: white;
              border: 2px solid rgba(76, 175, 80, 0.5);
            }
            ha-button {
              color: white;
            }
      - type: button
        name: Départ 08h00
        icon: mdi:clock-outline
        tap_action:
          action: call-service
          service: number.set_value
          service_data:
            entity_id: number.bmw_ix3_departure_time
            value: 8
        card_mod:
          style: |
            ha-card {
              background: rgba(33, 150, 243, 0.2);
              border-radius: 15px;
              color: white;
              border: 2px solid rgba(33, 150, 243, 0.5);
            }
            ha-button {
              color: white;
            }
      - type: button
        name: Départ 10h00
        icon: mdi:clock-alert
        tap_action:
          action: call-service
          service: number.set_value
          service_data:
            entity_id: number.bmw_ix3_departure_time
            value: 10
        card_mod:
          style: |
            ha-card {
              background: rgba(255, 193, 7, 0.2);
              border-radius: 15px;
              color: white;
              border: 2px solid rgba(255, 193, 7, 0.5);
            }
            ha-button {
              color: white;
            }
  
  # Tableau des temps de charge
  - type: markdown
    content: |
      ## ⏱️ Temps de Charge (Mis à jour automatiquement)
      
      | Puissance | 80% | 100% |
      |-----------|-----|------|
      | **7.4 kW** | {{ states('sensor.bmw_ix3_charge_time_80_7_4kw') or 'Calcul...' }} | {{ states('sensor.bmw_ix3_charge_time_100_7_4kw') or 'Calcul...' }} |
      | **11 kW** | {{ states('sensor.bmw_ix3_charge_time_80_11kw') or 'Calcul...' }} | {{ states('sensor.bmw_ix3_charge_time_100_11kw') or 'Calcul...' }} |
      | **22 kW** | {{ states('sensor.bmw_ix3_charge_time_80_22kw') or 'Calcul...' }} | {{ states('sensor.bmw_ix3_charge_time_100_22kw') or 'Calcul...' }} |
      
      ---
      
      ### 📊 Configuration Actuelle
      - **SOC Cible** : {{ states('number.bmw_ix3_target_soc') or '80' }}%
      - **Heure de Départ** : {{ states('number.bmw_ix3_departure_time') or '08:00' }}h
      - **Arrêt Auto 80%** : {{ '✅ Activé' if is_state('switch.bmw_ix3_auto_stop_80_percent', 'on') else '❌ Désactivé' }}
    card_mod:
      style: |
        ha-card {
          background: rgba(255, 255, 255, 0.05);
          backdrop-filter: blur(10px);
          border-radius: 15px;
        }
        ha-markdown {
          padding: 20px;
        }
        table {
          width: 100%;
          border-collapse: collapse;
          margin: 20px 0;
        }
        th, td {
          padding: 12px;
          text-align: center;
          border: 1px solid rgba(255,255,255,0.2);
        }
        th {
          font-weight: bold;
          background: rgba(33, 150, 243, 0.3);
          color: white;
        }
        td {
          background: rgba(255, 255, 255, 0.1);
          color: white;
        }
        h2 {
          color: #2196F3;
          margin-top: 0;
        }
        h3 {
          color: #4CAF50;
          margin-top: 25px;
        }

# Style général
card_mod:
  style: |
    ha-card {
      gap: 10px;
    }

